document.addEventListener("DOMContentLoaded", function() {
    const particlesConfig = {
        // ... (all the configuration code you provided)
    };

    // Initialize particles
    particlesJS('particles-js', particlesConfig);

    // Enhanced smooth scroll effect for particles
    let scrolling = false;
    let mouseX = 0;
    let mouseY = 0;

    // ... (all the event listeners and animation code)
}); 